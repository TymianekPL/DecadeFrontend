interface HomeProps {
    path?: string
}

const Home = ({}: HomeProps) => (
    <div>
        <h1>Welcome to the Home Page</h1>
        {/* Your home page content */}
    </div>
);

export default Home;
